// rewrite these are actual reducers????
// and then do the combination in index????
import createReducer from './create-reducer'
const initialState = {}
const actionHandlers = {}

export default createReducer(initialState, actionHandlers)
